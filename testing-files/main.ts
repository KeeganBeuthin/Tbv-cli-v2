
  export function execute_credit_leg(amount: number, account: string): void {
    console.log(`Crediting ${amount} to account ${account}`);
  }
  
export function execute_debit_leg(amount: number, account: string): void {
    console.log(`Debiting ${amount} from account ${account}`);
  }
  
  export function http_request(a: number, b: number): number {
    return a + b;
  }
  
